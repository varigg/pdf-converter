# Per-issue Codex review state (thread ids, body buffers, last review
# text, raw event logs). Local-only — these are session artifacts, not
# source-of-truth artifacts; the issue on GitHub is the truth.
*
!.gitignore
